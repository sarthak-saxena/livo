import React from "react";
interface Props {
    children: React.ReactNode;
    className?: string;
}
declare const Typography: ({ children, className }: Props) => JSX.Element;
export default Typography;
